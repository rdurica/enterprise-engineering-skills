# Image skills

Personal image helpers. `/setup` does **not** vendor this folder into project repos.

```
img/          — generate or regenerate assets (`/img`)
img-upscale/  — Real-ESRGAN 4× upscale with a soft finish (`/img-upscale`)
```
