# Refactor Candidates

After the TDD cycle:

- Duplication → extract
- Long methods → private helpers (tests stay on public interface)
- Primitive obsession → value objects
- Existing code the new tests reveal as problematic

Run tests after each refactor step.
